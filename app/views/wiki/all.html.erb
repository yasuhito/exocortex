<%- @title = "All Pages" -%>

<%= page_title @title %>

<%= render :file => "navigation_bar" %>

<ul>
  <%- @all_pages.sort.each do | each | -%>
    <li><%= link_to each.name, :action => "view", :id => each.wiki_name %>
  <%- end -%>
</ul>
